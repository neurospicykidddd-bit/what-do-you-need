# your anthropic api key. never commit the real .env file.
# locally: copy this to .env and fill it in.
# on vercel: add this as an environment variable in project settings.
ANTHROPIC_API_KEY=sk-ant-...
